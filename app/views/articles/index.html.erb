<p style="color: green"><%= notice %></p>

<h1>Lista e deshirave</h1>

<div id="articles" class="d-flex flex-column gap-2">
  <% @articles.each do |article| %>
    <%= render article %>
  <% end %>
</div>

<%= link_to "Krijo", new_article_path, class: "btn--link mt-2 rounded" %>
